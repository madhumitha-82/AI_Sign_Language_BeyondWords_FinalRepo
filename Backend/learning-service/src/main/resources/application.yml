server:
  port: 8082
  tomcat:
    accesslog:
      enabled: true
      directory: /tmp/tomcat-logs
      prefix: access_log
      suffix: .log
      pattern: common

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/beyondwords_learning?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: madhumitha_15
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always

app:
  jwt:
    secret: beyondwords-super-secret-key-change-me
    expiration: 86400000
  user-service:
    url: http://localhost:8081
